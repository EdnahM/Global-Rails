[project]
name = "african-rails-frontend"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.100.0",
    "uvicorn>=0.24.0",
    # ... any other frontend deps
]

[tool.vercel]
entrypoint = "api:app"   

[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    unit: pure unit tests, no network
    integration: read-only live network calls
    e2e: testnet transactions, requires funded wallet

addopts =
    -v
    --tb=short
    -m "not e2e"
